eureka:
  client:
    service-url:
      # 指定eureka服务器的地址
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    # 指定进行服务注册时该服务的名称
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: release
  rabbitmq:
    addresses: 132.232.100.191:5672
    username: guest
    password: guest
    virtual-host: /
    listener:
      simple:
        acknowledge-mode: manual
        concurrency: 5
        max-concurrency: 10
